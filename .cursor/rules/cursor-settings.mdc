---
description: Cursor IDE manual settings (OpenAI API key, Base URL, model aliases)
globs: "**/README.md"
alwaysApply: false
---

# Cursor Settings Rules

## Manual approach (current default)

The gateway does **not** automatically modify Cursor's `state.vscdb`. You set
three things manually in Cursor Settings → Models:

| Setting                       | Value                                                                          |
| ----------------------------- | ------------------------------------------------------------------------------ |
| OpenAI API Key                | Gateway key from `start` JSON (`gateway_key`) or `status` JSON (`gateway_key_masked` to verify) |
| Override OpenAI Base URL      | `https://<your-tunnel-host>/v1` (or your BYO HTTPS URL)                       |
| Model                         | An alias from the model table below (e.g. `kimi-k3`, `deepseek-v4-pro`)    |

**Provider choice is the alias** (Kimi vs DeepSeek) — Cursor still talks only to
the gateway; never to Moonshot/DeepSeek directly.

## Model alias table

Primary Agent-safe aliases (see `gateway.mdc` for full map):

| Alias            | →                                              |
| ---------------- | ---------------------------------------------- |
| `kimi-k3`        | Moonshot `kimi-k3` (planning; **prefer** — Cursor 1M context default) |
| `kimi-k2.7-code` | Moonshot `kimi-k2.7-code` (coding; always thinks; **prefer**) |
| `deepseek-v4-pro` | DeepSeek pro (hard execution; **prefer**) |
| `deepseek-v4-flash-vision-exp` | DeepSeek flash (cheap execution; native vision; **prefer**) |
| `thaura`         | Thaura `thaura` (ethical AI; optional)         |
| `glm-5.3`        | Z.AI `glm-5.3` (planning; always thinks; **prefer**) |
| `glm-5.3-flash`  | Z.AI `glm-5.3-flash` (cheap execution, 1M context; **prefer**) |
| `gpt-4-turbo`    | Z.AI `glm-5.3` (**compat** — Cursor may rewrite `gpt-4.1-turbo` to this) |
| ~~`gpt-4o`~~     | **Deprecated** — same as `kimi-k3`; Cursor ~128k context assumption |
| ~~`gpt-4o-mini`~~ | **Deprecated** — same as `kimi-k2.7-code`; Cursor ~128k context assumption |
| ~~`gpt-4.1-turbo`~~ | **Deprecated** — same as `glm-5.3`; Cursor assumes ~200k context |
| ~~`gpt-4.1`~~    | **Deprecated** — same as `glm-5.3-flash`; Cursor assumes ~200k context |
| ~~`o1`~~         | **Deprecated** — same as `deepseek-v4-pro`; Cursor assumes ~200k context |
| ~~`o3-mini`~~    | **Deprecated** — same as `deepseek-v4-flash-vision-exp`; Cursor assumes ~200k context |

> **Heads-up:** Cursor sometimes rewrites `gpt-4.1-turbo` → `gpt-4-turbo` before
> sending (client-side model-name matching). That is expected and handled — the
> gateway accepts `gpt-4-turbo` as an alias for `glm-5.3`. If you see
> `unknown model alias` in the gateway log, check the exact name Cursor sent
> (not what the picker displayed).

Do not use `claude-*` / `gemini-*` names on the OpenAI override path.

## Why manual?

- Cursor's `secret://cursorAuth/openAIKey` storage differs between versions
- Troubleshooting is easier when you can toggle the override in the UI
- You can switch between Cursor's own models and the gateway quickly

## `Cursor private network forbidden`

| Symptom                                    | Cause                                 | Fix                                                                           |
| ------------------------------------------ | ------------------------------------- | ----------------------------------------------------------------------------- |
| Base URL to localhost fails / SSRF blocked | Cursor cloud cannot reach private IPs | Use public HTTPS tunnel or BYO URL (`tunnel.mdc`); never "fix" with 127.0.0.1 |

<!-- last-verified: 2026-08 -->
