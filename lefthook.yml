# https://lefthook.dev/
# Install once: make install-hooks   (or: lefthook install)

pre-commit:
  parallel: false
  commands:
    verify:
      run: make verify && git add $(git diff --name-only --diff-filter=M)
